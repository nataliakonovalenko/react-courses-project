const initialState = {

};

export default function modalReducer(state = initialState, action) {
    switch (action.type) {
        case "MODAL":
            return state
        case "MODAL1":
            return state
        default:
            return state
    }
}